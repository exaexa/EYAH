\exercise{pencils=1}{Polymorph}{Write a function that replaces all type
variables in a type expression by fresh variables not used anywhere in the
program. (It also receives a list of all used type variables.)}

\exercise{pencils=1}{Multi-compose}{The right-associative function composition
operator \(\cdot\) (`dot') has type \[(\cdot) :: (b \to c) \to (a \to b) \to a
\to c \] Using previous functions, find the type of expression
\[(\cdot)\cdot(\cdot)\cdot(\cdot)\] }

