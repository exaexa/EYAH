\exercise{lambdas=1}{Void}{Write a function that returns the empty type
\texttt{()}. Can this function carry any reasonable information?}

\exercise{pencils=1}{First Second}{Write a function \texttt{f} so that it has
following type:

\texttt{f :: (a, ((b, (c, d)), e)) -> c}

Use \texttt{fst} and \texttt{snd} to construct it.
}
