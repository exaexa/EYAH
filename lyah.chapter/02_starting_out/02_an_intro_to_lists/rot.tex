\exercise{}{Rotate}{Write a function that rotates a list by $n$ elements ---
removes $n$ elements from the beginning of the list and appends them on the
end.}
\exercise{}{Rotate+}{Support cases where $n$ can be negative (rotate in the
other direction) or larger than the length of the list.}
