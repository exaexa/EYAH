\exercise{}{Div/Mod}{
Compute (using functions \texttt{div} and \texttt{mod}) the
greatest common divisor of 384288231873 and 10744168841679.

You can use \texttt{div} as an infix operator if you wrap it in backticks, like
\texttt{`div`}.
}
